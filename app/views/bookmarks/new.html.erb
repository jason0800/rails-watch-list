<h2>Add New Movie To List (Bookmark)</h2>

<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.label :comment %>
  <%= f.text_field :comment %>
  <%= f.association :movie %>
  <%= f.button :submit, class:"btn btn-primary"%>
<% end %>
